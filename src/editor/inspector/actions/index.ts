// Barrel for the tap-action editor — the inspector subsection that authors an
// ActionMap (tap / icon tap / hold / smart-click → behaviour). The integrator
// imports ActionEditor here and drops it into the Inspector's TileSettings.

export { ActionEditor, type ActionEditorProps } from './ActionEditor';
